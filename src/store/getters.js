export default {
   PRODUCTS(state) {
      return state.products;
   },
   CART(state) {
      return state.cart;
   },
   ISBURGERMENUOPEN(state) {
      return state.isBurgerMenuOpen;
   },
   WIDTH(state) {
      return state.width;
   },
   HEIGHT(state) {
      return state.height;
   },
   MENULIST(state) {
      return state.menuList;
   },
   MAINSLIDER(state) {
      return state.mainSlider;
   },
   ADVANTAGES(state) {
      return state.advantages;
   },
   ROOMSSLIDER(state) {
      return state.roomsSlider;
   },
   TIPSSLIDER(state) {
      return state.tipsSlider;
   },
   FOOTERLIST(state) {
      return state.footerList;
   },
};
